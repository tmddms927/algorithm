#include <stdio.h>
 
int main(main)
{
    int a, b, v, height = 0, day = 0;
    scanf("%d %d %d", &a, &b, &v);
    day = (v - b) / (a - b);
    if ((v - b) % (a - b) > 0)
        day++;
    printf("%d", day);
    return 0;
}
int main(main)
{
    int a, b, v, height = 0, day = 0;
    scanf("%d %d %d", &a, &b, &v);
    day = (v - b) / (a - b);
    if ((v - b) % (a - b) > 0)
        day++;
    printf("%d", day);
    return 0;
}
